# V3 추천 품질 목표

## 목적

V3 추천 품질 개선은 서로 다른 능력을 한 지표로 섞지 않고 다음 두 단계로 진행한다.

1. 모델이 사용자의 취향을 얼마나 잘 파악하는지 개선한다.
2. 파악한 취향을 바탕으로 실제 추천 후보를 얼마나 잘 찾아내는지 개선한다.

현재 작업은 1단계에 집중한다. 1단계가 충분히 안정되기 전에는 후보 생성 성능을 함께 조정하지 않는다.

## 1단계. 사용자 취향 파악과 선호 순위 튜닝

### 확인할 능력

사용자의 과거 행동을 학습한 모델이 이후 사용자가 본 영화들 사이의 상대적인 선호도를 얼마나 정확히 구분하는지 확인한다.

이 단계는 다음 질문에 답한다.

> 후보 영화가 주어졌을 때 모델이 사용자의 취향을 이해하고 더 선호할 영화를 위에 배치하는가?

### 현재 평가 방법

- MovieLens 평점을 현재 DB 영화에 매핑한다.
- 사용자마다 시간순으로 앞 70%를 학습 행동, 뒤 30%를 평가 구간으로 나눈다.
- 최소 100개 이상의 매핑 리뷰가 있는 사용자만 사용한다.
- 학습 구간 positive 20개 이상, 평가 구간 positive 10개 이상을 요구한다.
- `5.0=saved`, `3.5~4.5=pinned`, `0.5~1.5=passed`, `2.0~3.0=무시`로 변환한다.
- 동일 사용자와 DB 영화의 중복 매핑은 가장 최근 평점 하나만 유지한다.
- 평가 후보는 각 사용자의 뒤쪽 30% 영화로만 구성한다.
- 정답 순위는 실제 평점의 관련도 순서로 만든다.
- 평점 `3.5/4.0/4.5/5.0`은 관련도 `1/2/3/4`로 사용한다.
- `NDCG@10`, `NDCG@20`, `NDCG@30`처럼 10개 단위와 전체 평가 후보 수에서 측정한다.
- Recall은 이 단계에서 측정하지 않는다.

현재 사용자 규모 실험은 `100 -> 150 -> 200 -> 300 -> 500명` 누적 구조다. 작은 사용자 집합은 큰 사용자 집합에 그대로 포함된다. 각 규모의 모델은 동일한 최초 100명의 평가 구간으로 측정해 평가 대상 변경이 결과에 섞이지 않게 한다.

현재 표본은 취향이 지나치게 자주 바뀌는 사용자를 제외하고, 학습 구간 장르 엔트로피가 적격 사용자 중앙값보다 낮은 사용자로 구성한다. 활동량으로 유형을 나누지 않으며 대표 선호 장르는 분산한다.

### 현재 V3 하이브리드 결과

| 학습 사용자 | NDCG@10 | NDCG@20 | NDCG@30 |
| ---: | ---: | ---: | ---: |
| 100 | 0.3963 | 0.4701 | 0.5474 |
| 150 | 0.4221 | 0.4838 | 0.5603 |
| 200 | 0.4177 | 0.4824 | 0.5562 |
| 300 | 0.4135 | 0.4810 | 0.5554 |
| 500 | 0.3911 | 0.4677 | 0.5452 |

현재 표본에서는 150명 모델이 가장 높았으며, 사용자를 단순히 추가한다고 점수가 계속 오르지는 않았다.

### 1단계 고정 기준선: 순수 LightFM

동일한 사용자, 시간 분할, 평가 후보, NDCG 관련도와 random seed를 유지하고 모델만 다음과 같이 교체한 결과를 1단계 기준선으로 사용한다.

- 사용자·영화 identity만 사용하는 LightFM
- ontology item/user feature 미사용
- V3 collaborative confidence와 score centering 미사용
- `loss=logistic`
- `PASS=-1, confidence=1.0`
- `PIN=+1, confidence=1.0`
- `SAVED=+1, confidence=1.5`
- Neutral과 평점이 없는 영화는 학습에서 제외
- components 32, epochs 20, learning rate 0.01, user/item alpha 1e-5

| 학습 사용자 | NDCG@10 | NDCG@20 | NDCG@30 |
| ---: | ---: | ---: | ---: |
| 100 | 0.4279 | 0.4924 | 0.5699 |
| 150 | 0.4324 | 0.4958 | 0.5702 |
| 200 | 0.4277 | 0.4973 | 0.5700 |
| 300 | 0.4343 | 0.5055 | 0.5765 |
| 500 | 0.4380 | 0.5138 | 0.5790 |

순수 LightFM은 현재 V3 하이브리드보다 모든 사용자 규모의 NDCG@20과 NDCG@30에서 높았고, 사용자 수가 증가해도 크게 하락하지 않았다. 임의의 초기 설정으로 만든 단일 모델도 현재 하이브리드보다 높으므로, 현재 V3의 장기 취향 표현이나 학습 방식에 개선 여지가 있다고 판단한다.

### 500명 조정 결과

centering grid와 장기 positive 반감기 grid를 순서대로 수행해 centering을 `0.9 -> 0.5`로 조정했다. 초기 집중형 평가에서는 saved/pinned 반감기 `730일`이 가장 높았지만, 집중·안정형, 다취향·안정형, 취향변화형 각 100명의 후속 비교에서는 전체 기준 `365일`이 가장 높아 공통 기본값을 `365일`로 정했다. WARP, ontology feature, 협업 identity confidence, 영화 빈도 보정과 사용자 활동량 보정은 유지했다.

| 모델 | NDCG@10 | NDCG@20 | NDCG@30 |
| --- | ---: | ---: | ---: |
| 조정 전 V3 | 0.3911 | 0.4677 | 0.5452 |
| 순수 LightFM 기준선 | 0.4380 | 0.5138 | 0.5790 |
| 조정 후 V3 shadow | **0.4612** | **0.5244** | **0.5967** |
| 365일 활성 V3, 620명 | 0.4608 | 0.5221 | 0.5878 |

조정 후 V3 shadow 수치는 `730일`을 사용한 초기 집중형 500명 결과다. 현재 활성 모델은 `365일`과 620명 학습 데이터를 사용하며 순수 LightFM 기준선보다 NDCG@20 `+0.0082`, NDCG@30 `+0.0089` 높았다. 사용자 규모별 재측정과 반복 seed 검증은 남아 있으므로 1단계 전체 통과로 확정하지 않는다.

### 1단계 통과 목표

현재 V3 하이브리드 모델은 동일 조건에서 위 순수 LightFM 기준선을 넘어야 한다.

1. 각 사용자 규모의 NDCG@10, NDCG@20, NDCG@30을 같은 규모의 순수 LightFM과 비교한다.
2. 핵심 비교 지표는 NDCG@20과 NDCG@30으로 둔다.
3. 500명 기준으로 최소 `NDCG@20 > 0.5138`, `NDCG@30 > 0.5790`을 목표로 한다.
4. 특정 한 규모에서만 우연히 높아지는 것으로 통과 처리하지 않는다.
5. 100~500명 전체에서 평균적으로 기준선을 넘고, 사용자 증가에 따라 큰 품질 붕괴가 없어야 한다.
6. 동일 조건을 반복했을 때 개선 방향이 유지되어야 한다.

이 기준선은 현재 설정으로 고정한다. 이후 순수 LightFM 자체를 추가 튜닝한 결과는 별도 실험으로 기록하며, 현재 기준선을 소급해 바꾸지 않는다.

### 이 결과가 의미하는 것

- 모델의 장기 취향 표현과 영화 선호도 점수 구분 능력을 평가한다.
- LightFM 학습 가중치, feature 표현, 협업 신호와 사용자 표현을 튜닝하는 기준으로 사용한다.
- 보지 않은 영화를 싫어한다고 가정하지 않으므로 전체 카탈로그의 미노출 편향을 현재 평가에 넣지 않는다.

### 이 결과가 의미하지 않는 것

- 전체 영화 중 사용자가 좋아할 영화를 후보로 찾아냈다는 뜻이 아니다.
- 현재 NDCG가 높다고 실제 추천 후보 생성이 잘된다고 결론 내리지 않는다.
- 후보 병합, 단기 취향, OTT, 최종 정책까지 포함한 전체 서비스 품질을 증명하지 않는다.

### 튜닝 진행 원칙

1. 동일한 사용자, 시간 분할, 평가 후보와 random seed를 유지한다.
2. 한 번에 하나의 모델 또는 feature 조건만 변경한다.
3. NDCG@10, NDCG@20, NDCG@30과 사용자 규모별 변화를 함께 비교한다.
4. 평균 상승과 함께 특정 사용자군의 큰 하락 여부를 확인한다.
5. 무작위 순위, 영화 평균 평점순, 인기순과 같은 비개인화 NDCG 기준선을 마련한다.
6. LightFM 단독과 ontology feature 결합 모델을 같은 조건에서 비교한다.
7. 현재 평가가 사용하지 않는 후보 병합이나 서비스 정책을 NDCG 상승 목적으로 변경하지 않는다.
8. 평가용 shadow model은 운영 serving bundle에 자동으로 활성화하지 않는다.

순수 LightFM과 현재 V3의 차이는 한 번에 하나씩 분해한다.

1. logistic loss와 명시적 PASS 학습 효과
2. SAVED confidence 1.5 효과
3. ontology item feature 추가 효과
4. ontology user feature 추가 효과
5. 인기 영화·활동량 학습 감쇄 효과
6. collaborative confidence와 score centering 효과

단일 모델보다 점수가 낮은 원인을 확인하지 않은 상태에서 ontology 비중이나 최종 정책 점수를 임의로 올리지 않는다.

1단계의 최소 목표는 위 순수 LightFM 기준선을 넘는 것이다. 기준선 통과 후에도 여러 사용자 규모와 고정 seed 반복에서 개선이 유지되는지 확인하고, 추가 조정의 효과가 작아지는 시점에 2단계 진입을 판단한다. 최종 종료 수치는 이 반복 결과를 본 뒤 확정한다.

## 2단계. 추천 후보 생성 품질 개선

### 진입 조건

1단계에서 사용자의 상대적 선호도를 구분하는 능력이 충분한 수준에 도달한 뒤 시작한다. 점수 모델이 불안정한 상태에서 후보 생성까지 함께 수정하면 어느 단계가 결과를 개선하거나 악화했는지 구분할 수 없다.

### 확인할 능력

전체 적격 영화 카탈로그에서 사용자가 이후 선호한 영화를 실제 추천 후보군으로 얼마나 잘 찾아내는지 확인한다.

이 단계는 다음 질문에 답한다.

> 모델이 이해한 사용자 취향을 사용해 실제 추천 대상 영화를 후보군 안으로 가져오는가?

### 평가 방향

- 평가 후보를 뒤쪽 30% 영화로 제한하지 않는다.
- 전체 적격 카탈로그에서 실제 V3 후보 생성 경로를 실행한다.
- 뒤쪽 30%의 선호 영화가 Top-K 후보에 얼마나 포함되는지 Recall@K로 측정한다.
- LightFM, 장기 ontology, 단기 ontology와 cold-start 등 후보 source별 기여를 분리한다.
- 후보 수에 따른 Recall 변화와 후보 중복, catalog coverage, 인기 영화 과집중을 함께 확인한다.
- 후보 생성이 안정된 뒤에만 최종 병합과 정책 재정렬을 포함한 종단 평가를 추가한다.

2단계의 후보 수와 합격 기준은 1단계 종료 후 실제 서비스의 top-150 저장, 100개 상세 처리와 페이지 소비 범위를 기준으로 별도 확정한다.

## 두 단계의 해석 경계

| 구분 | 1단계 | 2단계 |
| --- | --- | --- |
| 핵심 질문 | 취향을 잘 파악했는가 | 취향에 맞는 후보를 잘 찾았는가 |
| 평가 후보 | 사용자가 실제로 본 뒤쪽 30% | 전체 적격 영화 카탈로그 |
| 중심 지표 | NDCG | Recall@K |
| 현재 상태 | 진행 중 | 1단계 완료 후 시작 |
| 주요 조정 대상 | 모델, 학습 신호, feature, 점수 | retrieval source, 후보 수, 병합과 검색 구조 |

두 결과는 서로 대체하지 않는다. 1단계 NDCG는 취향 파악 능력의 기준이고, 2단계 Recall은 그 취향을 실제 후보 생성으로 연결하는 능력의 기준이다.
